# NYC 311 Analytics — pinned environment (Python 3.11)
# Install:  python -m pip install -r requirements.txt
numpy==1.26.4
pandas==1.5.3

# Notebook authoring + headless execution (pulls nbconvert, nbformat, ipykernel)
notebook
